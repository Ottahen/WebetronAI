# Core
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
ddgs>=0.1.5
wikipedia>=1.4.0
python-dotenv>=1.0.0

# API server + UI
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Optional: natural LLM-generated answers (set OPENAI_API_KEY)
openai>=1.0.0

# Optional: fully local t5-small summarizer instead of the built-in
# extractive fallback (set SUMMARIZER_BACKEND=t5). Heavy (~2GB with
# PyTorch) — uncomment only if you want it.
# transformers>=4.40.0
# torch>=2.2.0
